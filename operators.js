let a = 10;
let b = 5;
let sum = a + b; //arithmetic operator (+)
let isGreater = a > b; //comparison operator (>)
let isTrue = true;
let isFalse = false;
let result = isTrue && isFalse; //logical operator (&&)
let c = 0;
c += a; //assignment operator (+=)
